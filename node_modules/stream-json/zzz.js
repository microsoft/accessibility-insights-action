const {chain}  = require('stream-chain');
const {make: verifier} = require('./utils/Verifier');

const fs = require('fs');

const pipeline = chain([
  fs.createReadStream('sample.json'),
  verifier()
]);

pipeline.on('error', error => console.log(error));
